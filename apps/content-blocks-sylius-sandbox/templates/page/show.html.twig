{% extends 'site.html.twig' %}

{% block title %}{{ page.title }} — Nova{% endblock %}

{% block content %}
    {% if page.contentArea %}
        {{ cb_render_content_area(page.contentArea) }}
    {% endif %}
{% endblock %}

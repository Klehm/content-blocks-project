framework:
    default_locale: fr
    translator:
        default_path: '%kernel.project_dir%/translations'
        # Safety net: any key missing from the fr catalog falls back to the
        # bundles' en catalog rather than rendering the raw key.
        fallbacks: ['en']
        providers:

APP_ENV=dev
APP_DEBUG=1
APP_SECRET=encore_sandbox_secret_change_me
DEFAULT_URI=http://localhost:8002

# The Encore sandbox uses its own database so it can be created, dropped and
# reset without disturbing the AssetMapper sandbox.
DATABASE_URL="mysql://content_blocks:your_password_here@127.0.0.1:3306/content_blocks_encore_sandbox?serverVersion=8.0&charset=utf8mb4"

{# Verbatim: this block trusts its editors, and is disabled by default.
   The wrapper is what kit.css scopes an editor-authored <img> to. #}
<div class="cb-block-html-raw">{{ data.html|default('')|raw }}</div>

{# Sanitized at render: import, templates and translations skip the form. #}
{% if data.content|default %}
    <div class="cb-block-rich-text">{{ data.content|cb_kit_rich_html }}</div>
{% else %}
    <small style="color: #5b6478;">{{ 'cb_kit.block.rich_text.empty'|trans({}, 'content_blocks_kit') }}</small>
{% endif %}
